source "https://rubygems.org"
ruby '2.2.3'

gem 'jekyll'        # blog static site generator
gem 'kramdown'      # Markdown converter
gem 'puma'          # concurrent web server, multi threaded
gem 'rack'          # build tool like make
gem 'rack-timeout'
gem 'rack-contrib'  # contributed rake middleware and utilities
# gem 'rack-jekyll'
